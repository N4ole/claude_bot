# `!gend`

Termine immédiatement un giveaway (par ID de message).

- **Catégorie** : 🎲 Utilitaire
- **Permission** : Administrateur
- **Disponible en** : préfixe (`!`) et slash (`/`)
- **Usage** : `!gend <message_id>`

> Légende : `<...>` argument obligatoire · `[...]` facultatif.
