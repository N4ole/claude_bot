# `!move`

Déplace un membre vers un autre salon vocal.

- **Catégorie** : 🛡️ Modération
- **Permission** : Déplacer des membres
- **Disponible en** : préfixe (`!`) et slash (`/`)
- **Usage** : `!move <member> <salon>`

> Légende : `<...>` argument obligatoire · `[...]` facultatif.
