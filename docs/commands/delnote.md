# `!delnote`

Supprime une note du dossier d'un utilisateur (par numéro).

- **Catégorie** : 🛡️ Modération
- **Permission** : Administrateur
- **Disponible en** : préfixe (`!`) et slash (`/`)
- **Usage** : `!delnote <member> <numero>`

> Légende : `<...>` argument obligatoire · `[...]` facultatif.
