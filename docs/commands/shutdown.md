# `§shutdown`

Éteint le bot.

- **Catégorie** : 👑 Owner
- **Permission** : Owner du bot
- **Disponible en** : préfixe (`§`) et slash (`/`)
- **Usage** : `§shutdown`

> Légende : `<...>` argument obligatoire · `[...]` facultatif.
