# `§clear`

Supprime un nombre de messages du salon (max 100).

- **Catégorie** : 🛡️ Modération
- **Permission** : Gérer les messages
- **Disponible en** : préfixe (`§`) et slash (`/`)
- **Usage** : `§clear <nombre>`

> Légende : `<...>` argument obligatoire · `[...]` facultatif.
