# Copiez ce fichier en ".env" et remplissez les valeurs.

# Token du bot Discord (obligatoire)
DISCORD_TOKEN=votre_token_ici

# Préfixe PAR DÉFAUT des commandes (optionnel, "!" par défaut).
# Chaque serveur peut le personnaliser avec la commande `prefixe`.
COMMAND_PREFIX=!

# ID du serveur pour synchroniser les commandes slash instantanément
# pendant le développement (optionnel). Laissez vide pour une
# synchronisation globale (peut prendre jusqu'à 1h).
GUILD_ID=

# ID de l'owner principal du bot. Cet owner a tous les droits et ne peut
# pas être retiré via la commande rmowner. Les owners additionnels sont
# gérés via addowner / rmowner (stockés dans owners.json).
OWNER_ID=votre_id_discord_ici

# URL du dépôt GitHub du bot (optionnel). Utilisée pour les liens de PR
# dans les MP de notification de mise à jour envoyés aux owners.
REPO_URL=https://github.com/N4ole/Watcher

# Invitation vers le serveur de support (optionnel). Mentionnée dans le MP
# envoyé au propriétaire d'un serveur blacklisté (commande owner `banserv`).
SUPPORT_SERVER=

# --- Panel web (optionnel) ------------------------------------------------
# Renseignez CLIENT_ID et CLIENT_SECRET (portail développeur Discord >
# OAuth2) pour activer le panel web d'administration. Ajoutez l'URL de
# redirection ci-dessous dans les "Redirects" de l'application Discord.
OAUTH_CLIENT_ID=
OAUTH_CLIENT_SECRET=
OAUTH_REDIRECT_URI=http://localhost:8080/callback
# Écoute locale par défaut (sécurisé). Pour exposer publiquement, mettez
# 0.0.0.0 — de préférence derrière un reverse-proxy HTTPS (voir docs/AUDIT.md).
WEB_HOST=127.0.0.1
WEB_PORT=8080
# Secret de signature des sessions (généré automatiquement si vide).
WEB_SECRET=

# --- Déploiement automatique (informatif) ----------------------------------
# Le déploiement auto ne lit PAS ce fichier : configurez DEPLOY_BRANCH,
# DEPLOY_SERVICE, DEPLOY_PYTHON et DEPLOY_INTERVAL_MIN au niveau du
# lancement de scripts/deploy.sh (voir docs/DEPLOY.md).
